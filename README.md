# 🕵️‍♂️ Hidden Protocol Bot
Автономный модуль наблюдения. Связан с Архивом Вне Времени. Распознаёт агентов. Передаёт директивы. Ты внутри Протокола.

---
Telegram-бот для **скачивания видео с Instagram и TikTok**, с поддержкой логирования, ограничений по чатам и администрирования.  
Основан на **Aiogram 3** и **yt-dlp**.

---

## ⚙️ Возможности

- 📥 Скачивание видео с **Instagram Reels** и **TikTok**  
- 💬 Автоматическое определение: **личный чат** или **разрешённая группа**  
- 🧵 Отправка всех видео из групп в **один общий тред**  
- 🚫 Игнорирование сообщений из неразрешённых чатов  
- 🧠 Команды:
  - `/start` — активация
  - `/help` — справка
  - `/gpt` — доступно только администраторам
- 🪵 Логирование:
  - в консоль
  - в файл `logs/bot_log_YYYY-MM-DD.log`
  - в Telegram (через `TelegramLogHandler`)
- 🔒 Конфигурация через `.env`

---

## 🧩 Структура проекта
```bash 
    app/
├── main.py # Точка входа
├── config.py # Загрузка и проверка .env
├── handlers/
│ ├── coreHandlersCommand.py # /start, /help, /gpt, unknown
│ └── video.py # Логика скачивания видео
├── services/
│ └── download_video.py # Работа с yt_dlp
├── utils/
│ ├── logger.py # Основной логгер
│ ├── tg_log_handler.py # Отправка логов в Telegram
└── requirements.txt
```
---

## 📄 Переменные окружения (.env)

| Переменная | Обязательно | Описание |
|-------------|-------------|-----------|
| `TOKEN` | ✅ | Telegram Bot Token от [@BotFather](https://t.me/BotFather) |
| `API_KEY_JWT` | ❌ | Зарезервировано для будущего API |
| `HOST` | ❌ | Хост HTTP API |
| `PORT` | ❌ | Порт (по умолчанию 8000) |
| `ADMIN_IDS` | ✅ | ID админов через запятую, пример: `12345678,98765432` |
| `ALLOWED_GROUP_IDS` | ✅ | Разрешённые группы, пример: `-12345678` |
| `TOPIC_CHAT_ID` | ✅ | ID группы, где есть нужный тред |
| `TOPIC_THREAD_ID` | ✅ | ID темы (thread_id) в группе |
| `DOWNLOAD_DIR` | ❌ | Папка загрузок (по умолчанию `./downloads`) |
| `LOG_LEVEL` | ❌ | Уровень логирования (`DEBUG`, `INFO`, `WARNING`, `ERROR`) |
| `LOG_CHAT_ID` | ❌ | ID чата/канала для логов |
| `LOG_THREAD_ID` | ❌ | ID темы для логов |

---

## 🚀 Установка и запуск

1. **Клонировать проект**
   ```bash
   git clone https://github.com/<username>/hidden-protocol-bot.git
   cd hidden-protocol-bot
    ```
2. **Создать и активировать виртуальное окружение**
   ```bash
    python -m venv venv
    source venv/bin/activate  # Linux/Mac
    venv\Scripts\activate     # Windows
   ```
3. **Установить зависимости**
   ```bash
   pip install -r requirements.txt
    ```
4. **Создать файл `.env`** в корне проекта и заполнить его переменными окружения.
    ```bash
    TOKEN=123456:ABCDEF...
    ADMIN_IDS=12345678
    ALLOWED_GROUP_IDS=-12345678
    TOPIC_CHAT_ID=-12345678
    TOPIC_THREAD_ID=1234
    LOG_CHAT_ID=-12345678
    LOG_LEVEL=INFO
   ```
5. **Запустить бота**
   ```bash
    python -m app.main
    docker-compose up -d --build
    ```
6. requirements.txt
    ```bash 
    aiogram==3.13.1
    yt-dlp==2025.9.23
    python-dotenv==1.0.1
    pydantic==2.10.6
    typing_extensions==4.12.2
    tqdm==4.67.1
    requests==2.32.4
    uvicorn==0.32.0
   ```
---
# 🧠 Принцип работы

- Пользователь отправляет ссылку на видео.
- Бот проверяет контекст:
- Личные сообщения: скачивает и отвечает пользователю.
- Разрешённые группы: скачивает и отправляет в указанный TOPIC_THREAD_ID.
- Видео скачивается через yt-dlp, временно сохраняется и отправляется как видео-сообщение.
- После отправки временный файл удаляется.
---
# 🧾 Формат логов

- Формат:
```Bash
  2025-11-02 14:05:33 [INFO] hidden_protocol
```
- Хранятся в logs/
- Ошибки и уведомления дублируются в Telegram-тред
---
# 💡 Пример сообщения
```Bash
  Пользователь: @example_user 
  Ссылка: https://www.tiktok.com/@example/video/1234567890
```
---
# 🔮 Планируемые возможности
- API-ручка для отправки сообщений в чат
- Расширенные права администраторов
- Подключение GPT чата
```Bash

- ```